{
  "name": "offensive-vk",
  "version": "10.0.1",
  "description": "My Personal Github Repository. (Fully Automated)",
  "main": "README.md",
  "bot": "https://github.com/TheHamsterBot",
  "scripts": {
    "install": "npx pnpm add @octokit/rest @octokit/core @octokit/types octokit markdownlint pnpm",
    "source": "git clone https://github.com/offensive-vk/offensive-vk.git",
    "dev": "sleep 5; echo 'Development Server Started...' ",
    "fix": "pnpx prettier --write . && pnpx prettier --check .",
    "image": "docker build -t offensive-vk .",
    "list": "docker ps -a",
    "info": "docker system info",
    "compose": "docker compose up --build -d",
    "pull": "docker pull ghcr.io/offensive-vk/offensive-vk:nightly",
    "exec": "docker exec -it my-container neofetch",
    "docker": "docker run -it -d --name docker-in-docker -p 9999:9999 --privileged -v /var/run/docker.sock:/var/run/docker.sock docker:dind",
    "spawn": "docker run -it -d --name my-container --rm -p 9999:9999 -v $(pwd):/shared/offensive-vk offensive-vk",
    "reset": "docker system prune -f",
    "all": "pnpm run install; pnpm run dev; pnpm run image; pnpm run info; pnpm run pull; pnpm run compose; pnpm run spawn; pnpm run list; pnpm run exec"
  },
  "keywords": [
    "readme",
    "github",
    "profile",
    "daily",
    "automated",
    "yaml",
    "bot-managed"
  ],
  "author": {
    "name": "Vedansh ✨",
    "url": "https://github.com/offensive-vk/"
  },
  "license": "MIT",
  "packageManager": "pnpm@9.0.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/offensive-vk/"
  },
  "bugs": {
    "url": "https://github.com/offensive-vk/issues"
  },
  "homepage": "https://github.com/offensive-vk",
  "dependencies": {
    "@octokit/core": "^6.1.2",
    "@octokit/rest": "^21.0.2",
    "@octokit/types": "^13.5.0",
    "markdownlint": "^0.35.0",
    "octokit": "^4.0.2",
    "pnpm": "9.0.0"
  }
}
